Design 1-5 mutually exclusive variants of this end-to-end workflow:

{{ workflow_prompt }}

Parent Oracle domain: {{ parent_domain }}
Shared world: {{ shared_context }}
Relevant schema: {{ schema_context }}
Authoritative workflow specifications:
{{ workflow_context }}

Supported operations: {{ allowed_steps }}
Supported exceptions: {{ allowed_exceptions }}

Return JSON only matching:
{{ json_output }}

Each variant must describe one plausible path from its trigger to a terminal
outcome. Use only the supported operations and exceptions supplied above, and
list operation objects in causal order. Optional parameters must be simple,
declarative values such as portion, delay_days, or reason; never create IDs,
dates, money, SQL, or record contents. Give weights totaling approximately 1.
Cadence must be daily, weekly, monthly, quarterly, or annual. Use concise
snake_case names. Give each variant a concise, plain-language, one-sentence
description of what makes that path distinct. Set exception to null for a standard path. Do not create
entities, records, IDs, amounts, balances, totals, or journal entries.
